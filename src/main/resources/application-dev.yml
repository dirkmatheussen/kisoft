# Development profile - lokale ontwikkeling zonder Bearer token of UI login
knapp:
  mock:
    bypass-auth: true
    ui-auth-enabled: false

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://login.microsoftonline.com/00000000-0000-0000-0000-000000000000/v2.0
